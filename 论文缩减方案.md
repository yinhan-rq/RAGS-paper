# RAGS 论文缩减方案（ICPP 10页限制）

> 目标：将论文压缩至 ICPP 要求的 10 页以内，保留全部核心技术贡献。

---

## 当前论文结构与篇幅估计

| 章节 | 内容 | 当前篇幅估计 | 压缩潜力 |
|------|------|-------------|---------|
| Abstract | 摘要 | ~0.3 页 | 低 |
| §1 Introduction | 背景、挑战、贡献 | ~1 页 | 中 |
| §2 Motivation | Agent 特征、现有方法局限 | ~2.2 页 | **高** |
| §3 Algorithm Design | DynA + RL Dispatch | ~3 页 | 中 |
| §4 Implementation | KV Cache 实现、Workflow 支持 | ~0.8 页 | **高** |
| §5 Evaluation | 实验设置、结果、消融 | ~2.5 页 | 中 |
| §6 Related Work | 相关工作 | ~0.5 页 | 低 |

---

## 方案一：§2 Motivation 大幅精简（预计节省 ~0.7 页）⭐⭐⭐

### 1.1 §2.1 Agent Workflow 特征描述（节省约 0.3 页）

**问题：** 三个特征（Dynamic topology、Heterogeneous cost、KV cache affinity）各有一段详细描述，与 Introduction 第 1-2 段存在大量重叠。

**操作：**
- 删除 §2.1 中所有重复铺垫语句（如 "Our analysis reveals that Agent workflows deviate substantially from..."）
- 将三段特征描述**合并为一个 `\begin{itemize}` 列表**，每条 1-2 句话
- Figure 1 的三个子图（chatbot / summarization / code gen）考虑缩小整体高度，或适当压缩 `\vspace{0.5cm}` 等间距

**参考行号：** `lines 442–473`

---

### 1.2 §2.2 Queuing Limitations 数值算例（节省约 0.25 页）

**问题：** Figure 2 已经可视化了对比结果，正文中重复推导所有数值：

```
Under FCFS, all four jobs... average completion time of (10+10+11+6)/4 = 9.25 s.
Topo assigns... (10+9+11+1)/4 = 7.75 s.
An E2E-cost-aware policy... (15+7+3+1)/4 = 6.5 s.
```

**操作：**
- 删除具体的算术推导过程（约 5-6 行）
- 保留结论句，如：*"As shown in Figure~\ref{fig:queue-motivation}, DynA achieves a X\% reduction in average completion time over FCFS and Topo."*

**参考行号：** `lines 522–534`

---

### 1.3 §2.3 Dispatch 策略数值算例（节省约 0.15 页）

**问题：** 类似地，详细列出了所有具体数字（660 ms / 180 ms / 720 ms / 660 ms），而 Figure 3 已有图示。

```
SA forces all concurrent requests onto one instance, yielding 100 + 7×80 = 660 ms,
whereas RR distributes them for true parallelism, achieving only 100 + 80 = 180 ms.
```

**操作：**
- 将两段数值推导压缩为一句话结论，直接引用图
- 节省约 3-4 行

**参考行号：** `lines 562–578`

---

## 方案二：§4 Implementation 大幅压缩（预计节省 ~0.4 页）⭐⭐⭐

### 2.1 删除 KV Cache 树节点 bullet list（节省约 6 行）

**问题：** 当前描述了树节点三个字段（`token sequence` / `ref` / `block`），属于标准前缀树的工程实现细节，与调度算法核心贡献无直接关联。

**操作：**
- 删除 `\begin{itemize}...\end{itemize}` 的三个 bullet points
- 替换为一句话内联描述，例如：*"Each node stores the token sequence, a reference count for memory reclamation, and a block index mapping to the physical KV store."*

**参考行号：** `lines 1072–1081`

---

### 2.2 删除 PagedAttention block-alignment 段落（节省约 4 行）

**问题：** "To support PagedAttention, the Local Prefix Cache Tree adopts a block-granularity management strategy..." 这段描述对算法结论无直接支撑。

**操作：**
- 直接删除该段落（约 4 行）

**参考行号：** `lines 1083–1089`

---

### 2.3 §4.2 Agent Workflow Support 压缩（节省约 3 行）

**操作：**
- `UnifiedRequest` 的继承关系描述压缩为一句话
- 保留 workflow graph 触发机制的核心描述

**参考行号：** `lines 1117–1132`

---

## 方案三：§3 Algorithm Design 适度精简（预计节省 ~0.3 页）⭐⭐

### 3.1 状态空间 bullet list 精简（节省约 5 行）

**问题：** Request state（4 个 bullet）和 Cluster state（6 个 bullet）描述过于展开。

**操作：**
- **Request state**：将 $\vec{p}, \vec{d}, \vec{u}, \vec{\kappa}$ 四项合并为一句话描述
  ```
  The request feature matrix X^req comprises input length, estimated output length,
  a parallel-stage indicator, and the workflow-type identifier.
  ```
- **Cluster state**：将 $\vec{m}, \vec{g}, \vec{wq}, \vec{rq}, c, s, b$ 七项压缩为 3-4 行的紧凑段落，删去部分冗余说明

**参考行号：** `lines 870–906`

---

### 3.2 Policy Network 方程链适度合并（节省约 3 行）

**问题：** 公式 (7)–(10) 是标准 Transformer 前向传播的逐步展开，对了解 Transformer 的读者无额外信息量。

**操作：**
- 保留最关键的输出公式 $Q(s, a_j)$（公式 10）
- 将 Linear 映射（公式 7）、type embedding（公式 8）、TransformerEncoder（公式 9）合并为一段文字描述，省去3个独立公式块

**参考行号：** `lines 978–1007`

---

## 方案四：§1 Introduction 轻度精简（预计节省 ~0.15 页）⭐

### 4.1 删除 "remainder of this paper" 段落（节省 4 行）

**问题：** ICPP 论文不强制要求这段结构说明，且内容已在贡献列表中隐含。

**操作：**
- 直接删除 `lines 376–382` 的结构说明段落

---

### 4.2 贡献列表适度合并（节省约 3 行）

**问题：** 第 1 条（DynA 算法）和第 3 条（实验结果）在 Abstract 中已经完整呈现，存在冗余。

**操作：**
- 将第 3 条 bullet（实验结果）并入第 1、2 条的描述尾句中，从 3 条压缩为 2 条

**参考行号：** `lines 354–374`

---

## 方案五：§5 Evaluation 适度精简（预计节省 ~0.2 页）⭐⭐

### 5.1 Figure 4（token 分布）缩减子图数量（节省约 0.1 页）

**问题：** 当前 Figure 4 包含 4 个子图（ShareGPT / Wikidata / CodeAgent token 分布 + workflow request count 分布），占用大量版面。

**操作：**
- 保留最具代表性的 2 个子图（如 ShareGPT 串行 vs. CodeAgent 混合型，对比最鲜明）
- 其余数据集的统计特征用一句话文字概括

**参考行号：** `lines 1206–1242`

---

### 5.2 RL Training Configuration 压缩（节省约 4 行）

**问题：** 当前逐项列出所有超参数，占用约 8 行。

**操作：**
- 将超参数合并为 1-2 句话的紧凑描述，或使用单行 inline 列表
  ```
  We use lr=5e-5, γ=0.995, replay buffer of 50k, mini-batch 256, ε from 0.5→0.01.
  ```

**参考行号：** `lines 1157–1168`

---

## 优先级汇总与预期收益

| 优先级 | 方案 | 具体操作 | 预计节省 |
|--------|------|---------|---------|
| ★★★ | 方案一（§2） | 三处数值算例的文字推导压缩 | ~0.4 页 |
| ★★★ | 方案二（§4） | KV Cache 树节点细节删除 + alignment 段删除 | ~0.25 页 |
| ★★ | 方案三（§3） | 状态空间 bullet list 精简 | ~0.2 页 |
| ★★ | 方案五（§5） | Figure 4 缩减子图 + 超参数压缩 | ~0.15 页 |
| ★ | 方案四（§1） | 删除 "remainder" 段落 + 合并贡献条目 | ~0.1 页 |
| **合计** | | | **~1.1 页** |

---

## 核心原则

1. **图已呈现的数值，正文无需重复推导** — §2 中所有数值算例均有对应图示
2. **实现层工程细节可大幅压缩** — §4 中树结构字段、内存对齐等细节不影响贡献评估
3. **与 Abstract / Introduction 重叠的内容删除** — §2.1 的特征描述与引言高度重叠
4. **标准 ML 流程公式可合并描述** — Transformer 前向传播公式链无需逐步展开

---

*生成日期：2026-04-22*
